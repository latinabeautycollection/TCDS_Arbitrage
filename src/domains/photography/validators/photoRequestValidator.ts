import { z } from 'zod';
export const PhotoInputSchema = z.object({ uri: z.string().optional(), sourceUrl: z.string().optional(), filename: z.string().optional(), role: z.enum(['HERO','FRONT','BACK','LEFT','RIGHT','TOP','BOTTOM','SERIAL','DEFECT','ACCESSORY','PACKAGING','LABEL','UNKNOWN']).optional(), sortOrder: z.number().optional() }).refine(v=>v.uri || v.sourceUrl, 'uri or sourceUrl is required');
export const PhotoProcessRequestSchema = z.object({ context: z.object({ candidateId: z.number().optional(), listingId: z.string().uuid().optional(), sourceListingNormalizedId: z.number().optional(), ebayListingDraftFk: z.number().optional(), categoryKey: z.string().optional(), title: z.string().optional(), brand: z.string().optional(), model: z.string().optional(), conditionText: z.string().optional(), correlationId: z.string().optional() }), photos: z.array(PhotoInputSchema).min(1).max(24), priority: z.number().optional(), idempotencyKey: z.string().optional() });
export const ReviewDecisionSchema = z.object({ decision: z.enum(['APPROVED_OVERRIDE','REJECTED_CONFIRMED','NEEDS_RESHOOT','DISMISSED']), reviewerId: z.string().min(1), notes: z.string().min(1) });
