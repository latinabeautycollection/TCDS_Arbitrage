test('external references never accept before/after payload through API model',()=>{const route=require('node:fs').readFileSync(require('node:path').resolve(__dirname,'../../domains/governance/routes/accountabilityRoutes.ts'),'utf8');const part=route.split("/governance/mutations/external-reference")[1]??'';expect(part.slice(0,2500)).not.toContain('beforeState');expect(part.slice(0,2500)).not.toContain('afterState');});
