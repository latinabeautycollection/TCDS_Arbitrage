import fs from 'node:fs';import path from 'node:path';test('11E routes stay governance namespaced',()=>{const s=fs.readFileSync(path.resolve(__dirname,'../../domains/governance/routes/accountabilityRoutes.ts'),'utf8');expect(s).toContain('/governance/lineage/');expect(s).toContain('/governance/audit/');expect(s).not.toContain("r.get('/health'");expect(s).not.toContain("r.get('/ready'");});
