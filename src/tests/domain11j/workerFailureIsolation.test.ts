import{ControlPlaneWorker}from'../../domains/governance/workers/controlPlaneWorker';
test('one rule failure does not stop later rules and failed rule is not marked consumed',async()=>{const marks:any[]=[];const repo:any={componentId:async(code:string)=>code==='DOMAIN11J_RECOVERY_ENGINE'?'123e4567-e89b-42d3-a456-426614174001':'123e4567-e89b-42d3-a456-426614174000',triggerCandidates:async()=>[{authority:'11I',reference:'x',correlationId:null}],activeRules:async()=>[{ruleCode:'A',triggerAuthority:'11I',controlRuleId:'1',policyVersionId:'p',ruleSha256:'s'},{ruleCode:'B',triggerAuthority:'11I',controlRuleId:'2',policyVersionId:'p',ruleSha256:'s'}],markRuleProcessed:async(_c:any,r:any,o:any)=>marks.push([r.ruleCode,o]),approvedRecoveries:async()=>[],actionAckEvents:async()=>[],expiredControls:async()=>[]};let calls=0;const service:any={evaluate:async()=>{calls++;if(calls===1)throw new Error('unexpected')}};const logger:any={info:()=>{},warn:()=>{},error:()=>{}};const worker=new ControlPlaneWorker(repo,service,logger,{reference:'w',authSessionId:'x',permissions:['governance.control.evaluate']});await worker.tick(10);expect(calls).toBe(2);expect(marks).toEqual([['B','MATCHED']])});
