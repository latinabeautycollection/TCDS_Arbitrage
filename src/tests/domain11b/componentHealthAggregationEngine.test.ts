import { ComponentHealthAggregationEngine } from "../../domains/governance/engines/componentHealthAggregationEngine";
import type { ActiveCheckSnapshot, HealthCheckDefinition } from "../../domains/governance/models/healthTypes";
const def=(required:boolean,degrades:boolean):HealthCheckDefinition=>({id:"00000000-0000-4000-8000-000000000001",componentId:"00000000-0000-4000-8000-000000000002",checkCode:"TEST.CHECK",version:1,mode:"PULL",kind:"HTTP",successState:"FUNCTIONAL",requiredForComponentHealth:required,degradesComponentHealth:degrades,intervalSeconds:30,timeoutMs:1000,observationTtlSeconds:60,failureThreshold:2,recoveryThreshold:2,jitterSeconds:0,configuration:{}});
describe("ComponentHealthAggregationEngine",()=>{
  it("marks expired required evidence unavailable",()=>{const now=new Date("2026-01-01T00:00:00Z");const c:ActiveCheckSnapshot={definition:def(true,true),latestState:"FUNCTIONAL",expiresAt:new Date(now.getTime()-1)};expect(new ComponentHealthAggregationEngine().aggregate(c.definition.componentId,[c],now).healthState).toBe("UNAVAILABLE");});
  it("marks expired optional degrading evidence degraded",()=>{const now=new Date("2026-01-01T00:00:00Z");const req:ActiveCheckSnapshot={definition:def(true,true),latestState:"FUNCTIONAL",expiresAt:new Date(now.getTime()+60000)};const opt:ActiveCheckSnapshot={definition:{...def(false,true),id:"00000000-0000-4000-8000-000000000003",checkCode:"TEST.OPTIONAL"},latestState:"FUNCTIONAL",expiresAt:new Date(now.getTime()-1)};expect(new ComponentHealthAggregationEngine().aggregate(req.definition.componentId,[req,opt],now).healthState).toBe("DEGRADED");});
});
