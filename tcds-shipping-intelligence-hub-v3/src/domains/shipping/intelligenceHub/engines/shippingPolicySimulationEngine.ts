import type { ShippingIntelligenceDecision } from "../models/decisionEvidence"; export class ShippingPolicySimulationEngine{simulate(input:{decisions:ShippingIntelligenceDecision[];applyPolicy:(d:ShippingIntelligenceDecision)=>ShippingIntelligenceDecision;simulationId:string;}){const changes=input.decisions.map(o=>{const s=input.applyPolicy(structuredClone(o));return{decisionId:o.decisionId,originalStatus:o.protection.status,simulatedStatus:s.protection.status,profitDeltaCents:s.profit.worstCaseNetProfitCents-o.profit.worstCaseNetProfitCents};}).filter(x=>x.originalStatus!==x.simulatedStatus||x.profitDeltaCents!==0);return{simulationId:input.simulationId,totalDecisions:input.decisions.length,changedDecisionCount:changes.length,expectedProfitDeltaCents:changes.reduce((a,x)=>a+x.profitDeltaCents,0),projectedClaimDelta:0,projectedLateDeliveryDelta:0,changes};}}
