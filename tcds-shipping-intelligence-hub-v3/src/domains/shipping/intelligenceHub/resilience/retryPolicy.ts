export async function executeWithRetry<T>(operation:(attempt:number)=>Promise<T>,options:{maxAttempts:number;initialDelayMs:number;maxDelayMs:number;jitterRatio:number},isRetryable:(error:unknown)=>boolean):Promise<T>{let last:unknown;for(let attempt=1;attempt<=options.maxAttempts;attempt++){try{return await operation(attempt);}catch(e){last=e;if(attempt>=options.maxAttempts||!isRetryable(e))throw e;const base=Math.min(options.maxDelayMs,options.initialDelayMs*2**(attempt-1));const jitter=base*options.jitterRatio*(Math.random()*2-1);await new Promise(r=>setTimeout(r,Math.max(0,base+jitter)));}}throw last;}
