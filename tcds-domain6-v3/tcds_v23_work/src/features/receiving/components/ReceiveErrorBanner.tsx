import { AlertTriangle, RefreshCw } from 'lucide-react';
export function ReceiveErrorBanner({ message, onRetry }: { message: string; onRetry?: () => void }) { return <div role="alert" className="rounded-2xl border border-red-200 bg-red-50 p-4"><div className="flex items-start gap-3"><AlertTriangle className="shrink-0 text-tcds-red"/><div className="flex-1"><p className="font-black text-red-900">Receiving action could not be completed</p><p className="mt-1 text-sm font-semibold text-red-800">{message}</p>{onRetry && <button onClick={onRetry} className="tcds-focus mt-3 inline-flex items-center gap-2 rounded-xl border border-red-200 bg-white px-3 py-2 text-sm font-black text-red-900"><RefreshCw size={15}/>Try again</button>}</div></div></div>; }
