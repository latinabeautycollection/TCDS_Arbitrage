import { X } from 'lucide-react';
import type { InventoryAction } from '../types/inventoryTypes';

export function InventoryActionSheet({ actions, onAction, onClose }: { actions: InventoryAction[]; onAction: (action: InventoryAction) => void; onClose: () => void }) {
  return <div className="fixed inset-0 z-50 flex items-end justify-center bg-black/40 p-3" role="dialog" aria-modal="true"><div className="w-full max-w-md rounded-t-[2rem] bg-white p-5 shadow-floating safe-bottom"><div className="flex items-center justify-between"><h2 className="font-display text-page font-black">Item Actions</h2><button onClick={onClose} className="rounded-xl border border-tcds-line p-2"><X size={18}/></button></div><div className="mt-4 space-y-2">{actions.map((action) => <button key={action.actionCode} disabled={!action.enabled} onClick={() => onAction(action)} className="w-full rounded-2xl border border-tcds-line bg-white p-4 text-left shadow-surface disabled:bg-neutral-100"><div className="flex items-center justify-between gap-3"><span className="font-black text-tcds-ink">{action.label}</span>{action.requiresApproval && <span className="rounded-full bg-tcds-gold/15 px-2 py-1 text-[10px] font-black text-tcds-goldDeep">Approval</span>}</div>{!action.enabled && action.disabledReason && <p className="mt-1 text-xs font-semibold text-tcds-muted">{action.disabledReason}</p>}</button>)}</div></div></div>;
}
