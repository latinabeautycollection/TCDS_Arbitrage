import { CheckCircle2, Circle, XCircle } from 'lucide-react';
import type { ReturnGate } from '../types/returnTypes';
export function ReturnGateCard({gates,onSelect}:{gates:ReturnGate[];onSelect:(g:ReturnGate)=>void}){return <div className="space-y-2">{gates.map(g=>{const Icon=g.state==='PASS'?CheckCircle2:g.state==='FAIL'?XCircle:Circle;return <button key={g.code} onClick={()=>onSelect(g)} className="tcds-focus flex min-h-14 w-full items-center gap-3 rounded-2xl border border-tcds-line bg-white p-3 text-left shadow-soft"><Icon size={19} className={g.state==='PASS'?'text-tcds-green':g.state==='FAIL'?'text-red-700':'text-tcds-muted'}/><span className="flex-1"><span className="block font-black text-tcds-ink">{g.label}</span>{g.detail&&<span className="block text-xs text-tcds-muted">{g.detail}</span>}</span><span className="text-xs font-black text-tcds-muted">{g.state}</span></button>})}</div>}
