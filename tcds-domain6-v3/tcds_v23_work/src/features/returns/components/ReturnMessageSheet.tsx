import type { ReturnMessage } from '../types/returnTypes'; export function ReturnMessageSheet({message,onClose,onAction}:{message:ReturnMessage;onClose:()=>void;onAction:(m:ReturnMessage)=>void}){return <div className="fixed inset-0 z-[80] flex items-end bg-black/55" role="dialog" aria-modal="true" aria-labelledby="return-message-title"><div className="w-full rounded-t-[2rem] bg-white p-5 pb-[calc(1.25rem+env(safe-area-inset-bottom))]"><p className="text-xs font-black uppercase tracking-[.2em] text-tcds-goldDeep">{message.code}</p><h2 id="return-message-title" className="mt-1 font-display text-xl font-black">{message.title}</h2><p className="mt-3 text-sm text-tcds-muted">{message.explanation}</p><div className="mt-4 rounded-xl bg-tcds-surface p-3"><p className="text-xs font-black uppercase text-tcds-muted">Required next step</p><p className="mt-1 font-bold">{message.nextStep}</p></div>{message.supportReference&&<p className="mt-3 text-xs text-tcds-muted">Support reference: {message.supportReference}</p>}<div className="mt-5 grid grid-cols-2 gap-2"><button onClick={onClose} className="tcds-focus min-h-12 rounded-2xl border border-tcds-line font-black">Close</button>{message.primaryAction&&message.primaryAction!=='NONE'&&<button onClick={()=>onAction(message)} className="tcds-focus min-h-12 rounded-2xl bg-tcds-black px-3 font-black text-white">Take action</button>}</div></div></div>}
