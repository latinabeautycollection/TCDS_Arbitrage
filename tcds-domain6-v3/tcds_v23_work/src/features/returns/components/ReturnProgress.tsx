import type { ReturnStage } from '../types/returnTypes'; const stages:ReturnStage[]=['IDENTIFY','PACKAGE_INSPECTION','CONTENTS','EVIDENCE_COMPARE','CONDITION_SAFETY','RISK_REVIEW','DISPOSITION','HANDOFF','COMPLETE']; export function ReturnProgress({stage}:{stage:ReturnStage}){const active=stages.indexOf(stage);return <div className="overflow-x-auto" aria-label="Return progress"><div className="flex min-w-max gap-2 pb-1">{stages.map((s,i)=><div key={s} className={`rounded-full px-3 py-2 text-xs font-black ${i<active?'bg-tcds-green text-white':i===active?'bg-tcds-black text-tcds-gold':'bg-tcds-surface text-tcds-muted'}`}>{i+1}. {s.replaceAll('_',' ')}</div>)}</div></div>}
