import { AlertTriangle, ChevronRight, ShieldAlert } from 'lucide-react'; import type { ReturnMessage } from '../types/returnTypes';
export function ReturnMessageCenter({messages,onSelect}:{messages:ReturnMessage[];onSelect:(m:ReturnMessage)=>void}){const blocking=messages.filter(m=>m.blocking);if(!messages.length)return null;return <section className="rounded-enterprise border border-red-300 bg-red-50 p-4" role="alert" aria-live="assertive"><div className="flex gap-3"><ShieldAlert className="shrink-0 text-red-700"/><div className="min-w-0 flex-1"><p className="font-display text-lg font-black">{blocking.length?`${blocking.length} blocking issue${blocking.length===1?'':'s'} require attention`:'Return guidance'}</p><div className="mt-3 space-y-2">{messages.slice(0,5).map(m=><button key={m.code} onClick={()=>onSelect(m)} className="tcds-focus flex min-h-12 w-full items-center gap-2 rounded-xl border border-red-200 bg-white p-3 text-left"><AlertTriangle size={17} className={m.severity==='CRITICAL'?'text-red-700':'text-amber-600'}/><span className="min-w-0 flex-1"><span className="block font-black">{m.title}</span><span className="block truncate text-xs text-tcds-muted">{m.nextStep}</span></span><ChevronRight size={17}/></button>)}</div></div></div></section>}
