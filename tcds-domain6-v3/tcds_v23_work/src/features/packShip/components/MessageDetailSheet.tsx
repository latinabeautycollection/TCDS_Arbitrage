import { X } from 'lucide-react';
import type { PackShipMessage } from '../types/packShipMessages';
export function MessageDetailSheet({ message, onClose, onPrimary, onSecondary }: { message: PackShipMessage | null; onClose: () => void; onPrimary?: () => void; onSecondary?: () => void }) {
  if (!message) return null;
  return <div className="fixed inset-0 z-50 flex items-end bg-black/45" role="dialog" aria-modal="true" aria-labelledby="pack-message-title"><div className="max-h-[88vh] w-full overflow-y-auto rounded-t-[2rem] bg-white p-5 pb-[calc(1.25rem+env(safe-area-inset-bottom))] shadow-2xl"><div className="mx-auto mb-4 h-1.5 w-12 rounded-full bg-tcds-line"/><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-black uppercase tracking-[.18em] text-tcds-gold">Operational guidance</p><h2 id="pack-message-title" className="mt-1 font-display text-xl font-black text-tcds-ink">{message.title}</h2></div><button onClick={onClose} aria-label="Close guidance" className="rounded-xl border border-tcds-line p-2"><X size={18}/></button></div><p className="mt-3 text-sm text-tcds-muted">{message.explanation}</p><div className="mt-4 rounded-2xl bg-tcds-surface p-4"><p className="text-xs font-black uppercase tracking-[.14em] text-tcds-muted">Required next action</p><p className="mt-1 text-sm font-bold text-tcds-ink">{message.nextAction}</p></div>{message.steps?.length ? <ol className="mt-4 space-y-2">{message.steps.map((step, index) => <li key={step} className="flex gap-3 rounded-xl border border-tcds-line p-3"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-tcds-black text-xs font-black text-white">{index + 1}</span><span className="text-sm font-semibold text-tcds-ink">{step}</span></li>)}</ol> : null}{message.supportReference && <p className="mt-4 text-xs font-semibold text-tcds-muted">Support reference: {message.supportReference}</p>}<div className="mt-5 grid gap-2 sm:grid-cols-2">{message.primaryActionLabel && <button onClick={onPrimary} className="min-h-14 rounded-2xl bg-tcds-black px-4 font-black text-white">{message.primaryActionLabel}</button>}{message.secondaryActionLabel && <button onClick={onSecondary} className="min-h-14 rounded-2xl border border-tcds-line bg-white px-4 font-black">{message.secondaryActionLabel}</button>}</div></div></div>;
}
