import { ChevronRight, ShieldAlert } from 'lucide-react';
import type { ExceptionRecord } from '../types/supervisorTypes';
export function ExceptionQueue({ exceptions, onOpen }: { exceptions: ExceptionRecord[]; onOpen: (value: ExceptionRecord) => void }) {
  if (!exceptions.length) return <div className="rounded-2xl bg-tcds-surface p-5 text-center"><p className="font-black text-tcds-ink">No open exceptions</p><p className="mt-1 text-sm text-tcds-muted">Warehouse exception queues are currently clear.</p></div>;
  return <div className="space-y-3">{exceptions.map(exception => <button key={exception.exceptionId} onClick={() => onOpen(exception)} className="tcds-focus enterprise-motion w-full rounded-2xl border border-tcds-line bg-white p-4 text-left shadow-surface">
    <div className="flex items-start gap-3"><div className={`grid h-10 w-10 shrink-0 place-items-center rounded-xl ${exception.severity === 'EMERGENCY' || exception.severity === 'CRITICAL' ? 'bg-red-100 text-tcds-red' : exception.severity === 'ERROR' ? 'bg-orange-100 text-orange-700' : 'bg-amber-100 text-tcds-warning'}`}><ShieldAlert size={19}/></div><div className="min-w-0 flex-1"><div className="flex items-center justify-between gap-2"><p className="truncate font-black text-tcds-ink">{exception.title}</p><ChevronRight size={17} className="shrink-0 text-tcds-gold"/></div><p className="mt-1 text-xs font-semibold text-tcds-muted">{exception.exceptionNumber} · {exception.domain} · {exception.status}</p><p className="mt-2 text-sm text-tcds-muted">{exception.summary}</p><div className="mt-3 flex flex-wrap gap-2 text-[10px] font-black uppercase tracking-[.12em]"><span className="rounded-full bg-tcds-surface px-2 py-1">{exception.facilityCode}</span>{exception.blocking && <span className="rounded-full bg-red-50 px-2 py-1 text-tcds-red">Blocking</span>}{exception.ownerName && <span className="rounded-full bg-tcds-surface px-2 py-1">Owner: {exception.ownerName}</span>}</div></div></div>
  </button>)}</div>;
}
